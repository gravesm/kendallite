define([
    'backbone'
], function(Backbone) {

/**
 * The Query model stores the current state of the Solr query.
 * @type {[type]}
 */
var Query = Backbone.Model.extend({

    sync: function() {}

});

return new Query();

});