These are the SVG files used to create the webfont for displaying various GIS
icons. The font can be regenerated using https://github.com/FontCustom/fontcustom.
The kendallite project root contains a fontcustom config and a manifest file
which will be used when running the fontcustom command from project root.
